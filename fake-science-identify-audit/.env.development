# 审核端 API：默认「浏览器直连后端」，避免 Vite /api 代理异常时出现历史接口 404。
# 修改端口后务必保存并重启：npm run dev
VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1

# 若你希望 API 仍走开发服务器代理（相对路径 /api/v1），请注释掉上一行 VITE_API_BASE_URL，并填写后端实际地址：
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:8000
